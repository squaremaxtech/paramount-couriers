import { userType } from '@/types'
import React from 'react'

export default function ViewUser({ seenUser, selectFunction }: { seenUser: userType, selectFunction?: () => void }) {
    return (
        <div>ViewUser</div>
    )
}
